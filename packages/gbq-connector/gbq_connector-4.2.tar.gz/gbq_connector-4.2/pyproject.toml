[build-system]
requires = ["setuptools==75.6.0"] # ["setuptools>=76.0"]
build-backend = "setuptools.build_meta"

[tools.pytest.ini_options]
log_cli = true
log_cli_level = "INFO"
addopts = "--cov=gbq_connector"
testpaths = ["tests", "unit_tests"]