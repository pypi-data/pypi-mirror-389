# This file is auto-generated by OpenAPI Python Client.
# Do not edit manually.

from http import HTTPStatus
from typing import Any

import httpx

from ... import errors
from ...client import AuthenticatedClient, Client
from ...models.action_schema_out import ActionSchemaOut
from ...models.http_validation_error import HTTPValidationError
from ...types import UNSET, Response, Unset


def _get_kwargs(
    cluster: str,
    namespace: str,
    kind: str,
    name: str,
    *,
    api_version: Unset | str = "v1",
) -> dict[str, Any]:
    params: dict[str, Any] = {}

    params["api_version"] = api_version

    params = {k: v for k, v in params.items() if v is not UNSET and v is not None}

    _kwargs: dict[str, Any] = {
        "method": "post",
        "url": f"/api/v1/openshift/workload-delete/{cluster}/{namespace}/{kind}/{name}",
        "params": params,
    }

    return _kwargs


def _parse_response(
    *, client: AuthenticatedClient | Client, response: httpx.Response
) -> ActionSchemaOut | HTTPValidationError | None:
    if response.status_code == 202:
        response_202 = ActionSchemaOut.from_dict(response.json())

        return response_202

    if response.status_code == 422:
        response_422 = HTTPValidationError.from_dict(response.json())

        return response_422

    if client.raise_on_unexpected_status:
        raise errors.UnexpectedStatus(response.status_code, response.content)
    else:
        return None


def _build_response(
    *, client: AuthenticatedClient | Client, response: httpx.Response
) -> Response[ActionSchemaOut | HTTPValidationError]:
    return Response(
        status_code=HTTPStatus(response.status_code),
        content=response.content,
        headers=response.headers,
        parsed=_parse_response(client=client, response=response),
    )


def sync_detailed(
    cluster: str,
    namespace: str,
    kind: str,
    name: str,
    *,
    client: AuthenticatedClient | Client,
    api_version: Unset | str = "v1",
) -> Response[ActionSchemaOut | HTTPValidationError]:
    """Openshift Workload Delete

     Initiates a delete of a specified OpenShift workload.

    This action triggers a delete of a workload (e.g., Pod, Deployment)
    within a given OpenShift cluster and namespace.

    Args:
        cluster (str): OpenShift cluster name
        namespace (str): OpenShift namespace
        kind (str): OpenShift workload kind. e.g. Job or ConfigMap
        name (str): OpenShift workload name
        api_version (Union[Unset, str]): OpenShift API version Default: 'v1'.

    Raises:
        errors.UnexpectedStatus: If the server returns an undocumented status code and Client.raise_on_unexpected_status is True.
        httpx.TimeoutException: If the request takes longer than Client.timeout.

    Returns:
        Response[Union[ActionSchemaOut, HTTPValidationError]]
    """

    kwargs = _get_kwargs(
        cluster=cluster,
        namespace=namespace,
        kind=kind,
        name=name,
        api_version=api_version,
    )

    response = client.get_httpx_client().request(
        **kwargs,
    )

    return _build_response(client=client, response=response)


def sync(
    cluster: str,
    namespace: str,
    kind: str,
    name: str,
    *,
    client: AuthenticatedClient | Client,
    api_version: Unset | str = "v1",
) -> ActionSchemaOut | HTTPValidationError | None:
    """Openshift Workload Delete

     Initiates a delete of a specified OpenShift workload.

    This action triggers a delete of a workload (e.g., Pod, Deployment)
    within a given OpenShift cluster and namespace.

    Args:
        cluster (str): OpenShift cluster name
        namespace (str): OpenShift namespace
        kind (str): OpenShift workload kind. e.g. Job or ConfigMap
        name (str): OpenShift workload name
        api_version (Union[Unset, str]): OpenShift API version Default: 'v1'.

    Raises:
        errors.UnexpectedStatus: If the server returns an undocumented status code and Client.raise_on_unexpected_status is True.
        httpx.TimeoutException: If the request takes longer than Client.timeout.

    Returns:
        Union[ActionSchemaOut, HTTPValidationError]
    """

    return sync_detailed(
        cluster=cluster,
        namespace=namespace,
        kind=kind,
        name=name,
        client=client,
        api_version=api_version,
    ).parsed


async def asyncio_detailed(
    cluster: str,
    namespace: str,
    kind: str,
    name: str,
    *,
    client: AuthenticatedClient | Client,
    api_version: Unset | str = "v1",
) -> Response[ActionSchemaOut | HTTPValidationError]:
    """Openshift Workload Delete

     Initiates a delete of a specified OpenShift workload.

    This action triggers a delete of a workload (e.g., Pod, Deployment)
    within a given OpenShift cluster and namespace.

    Args:
        cluster (str): OpenShift cluster name
        namespace (str): OpenShift namespace
        kind (str): OpenShift workload kind. e.g. Job or ConfigMap
        name (str): OpenShift workload name
        api_version (Union[Unset, str]): OpenShift API version Default: 'v1'.

    Raises:
        errors.UnexpectedStatus: If the server returns an undocumented status code and Client.raise_on_unexpected_status is True.
        httpx.TimeoutException: If the request takes longer than Client.timeout.

    Returns:
        Response[Union[ActionSchemaOut, HTTPValidationError]]
    """

    kwargs = _get_kwargs(
        cluster=cluster,
        namespace=namespace,
        kind=kind,
        name=name,
        api_version=api_version,
    )

    response = await client.get_async_httpx_client().request(**kwargs)

    return _build_response(client=client, response=response)


async def asyncio(
    cluster: str,
    namespace: str,
    kind: str,
    name: str,
    *,
    client: AuthenticatedClient | Client,
    api_version: Unset | str = "v1",
) -> ActionSchemaOut | HTTPValidationError | None:
    """Openshift Workload Delete

     Initiates a delete of a specified OpenShift workload.

    This action triggers a delete of a workload (e.g., Pod, Deployment)
    within a given OpenShift cluster and namespace.

    Args:
        cluster (str): OpenShift cluster name
        namespace (str): OpenShift namespace
        kind (str): OpenShift workload kind. e.g. Job or ConfigMap
        name (str): OpenShift workload name
        api_version (Union[Unset, str]): OpenShift API version Default: 'v1'.

    Raises:
        errors.UnexpectedStatus: If the server returns an undocumented status code and Client.raise_on_unexpected_status is True.
        httpx.TimeoutException: If the request takes longer than Client.timeout.

    Returns:
        Union[ActionSchemaOut, HTTPValidationError]
    """

    return (
        await asyncio_detailed(
            cluster=cluster,
            namespace=namespace,
            kind=kind,
            name=name,
            client=client,
            api_version=api_version,
        )
    ).parsed


from typing import Annotated

import typer

app = typer.Typer()


@app.command(
    help="""Initiates a delete of a specified OpenShift workload.

This action triggers a delete of a workload (e.g., Pod, Deployment)
within a given OpenShift cluster and namespace.""",
    rich_help_panel="Actions",
)
def openshift_workload_delete(
    ctx: typer.Context,
    cluster: Annotated[
        str, typer.Option(help="OpenShift cluster name", show_default=False)
    ],
    namespace: Annotated[
        str, typer.Option(help="OpenShift namespace", show_default=False)
    ],
    kind: Annotated[
        str,
        typer.Option(
            help="OpenShift workload kind. e.g. Job or ConfigMap", show_default=False
        ),
    ],
    name: Annotated[
        str, typer.Option(help="OpenShift workload name", show_default=False)
    ],
    api_version: Annotated[str, typer.Option(help="OpenShift API version")] = "v1",
) -> None:
    result = sync(
        cluster=cluster,
        namespace=namespace,
        kind=kind,
        name=name,
        api_version=api_version,
        client=ctx.obj["client"],
    )
    if "formatter" in ctx.obj and result is not None:
        output: Any = result
        if isinstance(result, list):
            output = [
                item.to_dict() if hasattr(item, "to_dict") else item for item in result
            ]
        elif hasattr(result, "to_dict"):
            output = result.to_dict()
        ctx.obj["formatter"](output)
