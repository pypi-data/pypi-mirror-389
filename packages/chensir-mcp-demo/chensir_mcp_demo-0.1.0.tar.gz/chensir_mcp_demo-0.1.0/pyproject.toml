[project]
name = "chensir-mcp-demo"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
authors = [
    { name = "chensir", email = "1791933520@qq.com" }
]
requires-python = ">=3.13"
dependencies = [
    "mcp[cli]>=1.20.0",
]

[project.scripts]
chensir-mcp-demo = "chensir_mcp_demo:main"

[build-system]
requires = ["uv_build>=0.9.7,<0.10.0"]
build-backend = "uv_build"
