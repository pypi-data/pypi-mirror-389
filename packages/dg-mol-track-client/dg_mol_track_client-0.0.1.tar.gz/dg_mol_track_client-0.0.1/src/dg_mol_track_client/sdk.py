"""Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT."""

from .basesdk import BaseSDK
from .httpclient import AsyncHttpClient, ClientOwner, HttpClient, close_clients
from .sdkconfiguration import SDKConfiguration
from .utils.logger import Logger, get_default_logger
from .utils.retries import RetryConfig
from dg_mol_track_client._hooks import SDKHooks
from dg_mol_track_client.types import OptionalNullable, UNSET
import httpx
import importlib
import sys
from typing import Optional, TYPE_CHECKING, cast
import weakref

if TYPE_CHECKING:
    from dg_mol_track_client.additions import Additions
    from dg_mol_track_client.admin import Admin
    from dg_mol_track_client.assayresults import AssayResults
    from dg_mol_track_client.assayruns import AssayRuns
    from dg_mol_track_client.assays import Assays
    from dg_mol_track_client.automapcolumns import AutoMapColumns
    from dg_mol_track_client.batches import Batches
    from dg_mol_track_client.compounds import Compounds
    from dg_mol_track_client.schema import Schema
    from dg_mol_track_client.schemabatches import SchemaBatches
    from dg_mol_track_client.search import Search
    from dg_mol_track_client.users import Users
    from dg_mol_track_client.validators import Validators


class MolTrackClient(BaseSDK):
    r"""MolTrack API: API for managing chemical compounds and batches"""

    auto_map_columns: "AutoMapColumns"
    users: "Users"
    schema_: "Schema"
    compounds: "Compounds"
    schema_batches: "SchemaBatches"
    additions: "Additions"
    batches: "Batches"
    assays: "Assays"
    assay_runs: "AssayRuns"
    assay_results: "AssayResults"
    validators: "Validators"
    admin: "Admin"
    search: "Search"
    _sub_sdk_map = {
        "auto_map_columns": ("dg_mol_track_client.automapcolumns", "AutoMapColumns"),
        "users": ("dg_mol_track_client.users", "Users"),
        "schema_": ("dg_mol_track_client.schema", "Schema"),
        "compounds": ("dg_mol_track_client.compounds", "Compounds"),
        "schema_batches": ("dg_mol_track_client.schemabatches", "SchemaBatches"),
        "additions": ("dg_mol_track_client.additions", "Additions"),
        "batches": ("dg_mol_track_client.batches", "Batches"),
        "assays": ("dg_mol_track_client.assays", "Assays"),
        "assay_runs": ("dg_mol_track_client.assayruns", "AssayRuns"),
        "assay_results": ("dg_mol_track_client.assayresults", "AssayResults"),
        "validators": ("dg_mol_track_client.validators", "Validators"),
        "admin": ("dg_mol_track_client.admin", "Admin"),
        "search": ("dg_mol_track_client.search", "Search"),
    }

    def __init__(
        self,
        server_url: str,
        client: Optional[HttpClient] = None,
        async_client: Optional[AsyncHttpClient] = None,
        retry_config: OptionalNullable[RetryConfig] = UNSET,
        timeout_ms: Optional[int] = None,
        debug_logger: Optional[Logger] = None,
    ) -> None:
        r"""Instantiates the SDK configuring it with the provided parameters.

        :param server_idx: The index of the server to use for all methods
        :param server_url: The server URL to use for all methods
        :param url_params: Parameters to optionally template the server URL with
        :param client: The HTTP client to use for all synchronous methods
        :param async_client: The Async HTTP client to use for all asynchronous methods
        :param retry_config: The retry configuration to use for all supported methods
        :param timeout_ms: Optional request timeout applied to each operation in milliseconds
        """
        client_supplied = True
        if client is None:
            client = httpx.Client(follow_redirects=True)
            client_supplied = False

        assert issubclass(
            type(client), HttpClient
        ), "The provided client must implement the HttpClient protocol."

        async_client_supplied = True
        if async_client is None:
            async_client = httpx.AsyncClient(follow_redirects=True)
            async_client_supplied = False

        if debug_logger is None:
            debug_logger = get_default_logger()

        assert issubclass(
            type(async_client), AsyncHttpClient
        ), "The provided async_client must implement the AsyncHttpClient protocol."

        BaseSDK.__init__(
            self,
            SDKConfiguration(
                client=client,
                client_supplied=client_supplied,
                async_client=async_client,
                async_client_supplied=async_client_supplied,
                server_url=server_url,
                retry_config=retry_config,
                timeout_ms=timeout_ms,
                debug_logger=debug_logger,
            ),
            parent_ref=self,
        )

        hooks = SDKHooks()

        # pylint: disable=protected-access
        self.sdk_configuration.__dict__["_hooks"] = hooks

        self.sdk_configuration = hooks.sdk_init(self.sdk_configuration)

        weakref.finalize(
            self,
            close_clients,
            cast(ClientOwner, self.sdk_configuration),
            self.sdk_configuration.client,
            self.sdk_configuration.client_supplied,
            self.sdk_configuration.async_client,
            self.sdk_configuration.async_client_supplied,
        )

    def dynamic_import(self, modname, retries=3):
        for attempt in range(retries):
            try:
                return importlib.import_module(modname)
            except KeyError:
                # Clear any half-initialized module and retry
                sys.modules.pop(modname, None)
                if attempt == retries - 1:
                    break
        raise KeyError(f"Failed to import module '{modname}' after {retries} attempts")

    def __getattr__(self, name: str):
        if name in self._sub_sdk_map:
            module_path, class_name = self._sub_sdk_map[name]
            try:
                module = self.dynamic_import(module_path)
                klass = getattr(module, class_name)
                instance = klass(self.sdk_configuration, parent_ref=self)
                setattr(self, name, instance)
                return instance
            except ImportError as e:
                raise AttributeError(
                    f"Failed to import module {module_path} for attribute {name}: {e}"
                ) from e
            except AttributeError as e:
                raise AttributeError(
                    f"Failed to find class {class_name} in module {module_path} for attribute {name}: {e}"
                ) from e

        raise AttributeError(
            f"'{type(self).__name__}' object has no attribute '{name}'"
        )

    def __dir__(self):
        default_attrs = list(super().__dir__())
        lazy_attrs = list(self._sub_sdk_map.keys())
        return sorted(list(set(default_attrs + lazy_attrs)))

    def __enter__(self):
        return self

    async def __aenter__(self):
        return self

    def __exit__(self, exc_type, exc_val, exc_tb):
        if (
            self.sdk_configuration.client is not None
            and not self.sdk_configuration.client_supplied
        ):
            self.sdk_configuration.client.close()
        self.sdk_configuration.client = None

    async def __aexit__(self, exc_type, exc_val, exc_tb):
        if (
            self.sdk_configuration.async_client is not None
            and not self.sdk_configuration.async_client_supplied
        ):
            await self.sdk_configuration.async_client.aclose()
        self.sdk_configuration.async_client = None
