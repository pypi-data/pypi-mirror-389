"""Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT."""

from __future__ import annotations
from .assay import Assay, AssayTypedDict
from .assayrundetail import AssayRunDetail, AssayRunDetailTypedDict
from .property import Property, PropertyTypedDict
from datetime import datetime
from dg_mol_track_client.types import (
    BaseModel,
    Nullable,
    OptionalNullable,
    UNSET,
    UNSET_SENTINEL,
)
from pydantic import model_serializer
from typing import List, Optional
from typing_extensions import NotRequired, TypedDict


class AssayRunResponseTypedDict(TypedDict):
    name: str
    assay_id: int
    id: int
    created_at: datetime
    description: NotRequired[Nullable[str]]
    assay: NotRequired[Nullable[AssayTypedDict]]
    assay_run_details: NotRequired[List[AssayRunDetailTypedDict]]
    properties: NotRequired[List[PropertyTypedDict]]


class AssayRunResponse(BaseModel):
    name: str

    assay_id: int

    id: int

    created_at: datetime

    description: OptionalNullable[str] = UNSET

    assay: OptionalNullable[Assay] = UNSET

    assay_run_details: Optional[List[AssayRunDetail]] = None

    properties: Optional[List[Property]] = None

    @model_serializer(mode="wrap")
    def serialize_model(self, handler):
        optional_fields = ["description", "assay", "assay_run_details", "properties"]
        nullable_fields = ["description", "assay"]
        null_default_fields = []

        serialized = handler(self)

        m = {}

        for n, f in type(self).model_fields.items():
            k = f.alias or n
            val = serialized.get(k)
            serialized.pop(k, None)

            optional_nullable = k in optional_fields and k in nullable_fields
            is_set = (
                self.__pydantic_fields_set__.intersection({n})
                or k in null_default_fields
            )  # pylint: disable=no-member

            if val is not None and val != UNSET_SENTINEL:
                m[k] = val
            elif val != UNSET_SENTINEL and (
                not k in optional_fields or (optional_nullable and is_set)
            ):
                m[k] = val

        return m
