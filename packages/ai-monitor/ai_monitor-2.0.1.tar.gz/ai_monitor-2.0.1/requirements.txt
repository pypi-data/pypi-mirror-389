# AI Monitor Requirements - v2.0
# Lightweight installation - NO torch dependency (~100MB total vs ~600MB)

# Core dependencies
numpy>=1.20.0
requests>=2.25.0
httpx>=0.24.0
tiktoken>=0.5.0
psutil>=5.9.0

# Optional: Prometheus metrics export
prometheus-client>=0.15.0

# Optional: Jaeger distributed tracing
opentelemetry-api>=1.22.0
opentelemetry-sdk>=1.22.0
opentelemetry-exporter-jaeger>=1.21.0

# NEW v2.0: Dashboard dependencies
flask>=2.0.0
flask-cors>=3.0.0

# NEW v2.0: Lightweight semantic analysis (NO torch!)
# Uses TF-IDF + cosine similarity instead of transformers
scikit-learn>=1.0.0
scipy>=1.7.0

# REMOVED: sentence-transformers (required torch - 500MB+)
# Users who need transformer-based analysis can install separately:
# pip install ai-monitor[transformers]

# For development and testing
pytest>=7.0.0
pytest-cov>=4.0.0
build>=0.10.0
twine>=4.0.0
