[build-system]
requires = ["setuptools>=61.0"]
build-backend = "setuptools.build_meta"

[project]
name = "miniomni"
version = "0.0.2"
description = "A universal utility toolkit by minipuding"
readme = "README.md"
requires-python = ">=3.8"
authors = [
  { name="minipuding" }
]
license = { text="MIT" }
dependencies = [
    # 如果有第三方库写这里，否则留空
]

[tool.setuptools.packages.find]
where = ["src"]