[build-system]
requires = ["setuptools", "wheel"]
build-backend = "setuptools.build_meta"

[project]
name = "ize"
version = "0.0.1"
description = "Adapterize CLI"
authors = [{ name = "Your Name" }]
readme = "README.md"
requires-python = ">=3.7"
license = { text = "MIT" }

