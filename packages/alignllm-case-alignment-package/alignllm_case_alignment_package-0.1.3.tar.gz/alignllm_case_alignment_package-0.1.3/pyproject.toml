[build-system]
requires = ["setuptools>=61.0"]
build-backend = "setuptools.build_meta"

[project]
name = "alignllm_case_alignment_package"
version = "0.1.3"
description = "A package for computing case alignment using embeddings - AlignLLM"
authors = [{name = "Ramitha Abeyratne"}]
requires-python = ">=3.8"
dependencies = ["numpy", "scikit-learn"]
readme = "README.md"