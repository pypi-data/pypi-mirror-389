[project]
name = "itgeeker"
version = "0.2.6"
description = "Build by ITGeeker.net"
readme = "README.md"
requires-python = ">=3.12"
dependencies = [
    "chardet>=5.2.0",
    "pytz>=2025.2",
    "requests>=2.32.4",
]
authors = [
    {name = "ITGeeker"}
]

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[tool.hatch.build.targets.wheel]
packages = ["src/itgeeker"]