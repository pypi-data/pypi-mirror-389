[build-system]
requires = ["setuptools>=45", "wheel"]
build-backend = "setuptools.build_meta"

[project]
name = "python-extension-toolkit"
version = "1.0.0"
description = "A Python CLI tool for extension development (Python equivalent of Zoho Extension Toolkit)"
readme = "README.md"
authors = [
    {name = "Python Extension Toolkit Team", email = "pet@example.com"}
]
license = {text = "MIT"}
classifiers = [
    "Development Status :: 5 - Production/Stable",
    "Intended Audience :: Developers",
    "License :: OSI Approved :: MIT License",
    "Programming Language :: Python :: 3",
    "Programming Language :: Python :: 3.8",
    "Programming Language :: Python :: 3.9",
    "Programming Language :: Python :: 3.10",
    "Programming Language :: Python :: 3.11",
    "Programming Language :: Python :: 3.12",
    "Topic :: Software Development :: Libraries :: Python Modules",
    "Topic :: Software Development :: Build Tools",
]
keywords = ["cli", "extension", "development", "toolkit", "pet", "zoho"]
requires-python = ">=3.8"
dependencies = [
    "click>=8.0.0",
    "flask>=2.0.0",
    "flask-cors>=4.0.0",
    "requests>=2.25.0",
]

[project.optional-dependencies]
dev = [
    "pytest>=6.0",
    "black",
    "flake8",
    "mypy",
]

[project.urls]
Homepage = "https://github.com/Aqary-Org/python-extension-toolkit"
Documentation = "https://github.com/Aqary-Org/python-extension-toolkit#readme"
Repository = "https://github.com/Aqary-Org/python-extension-toolkit.git"
"Bug Tracker" = "https://github.com/Aqary-Org/python-extension-toolkit/issues"

[project.scripts]
pet = "pet.cli:cli"

[tool.setuptools.packages.find]
where = ["."]
include = ["pet*"]
exclude = ["tests*"]

[tool.black]
line-length = 88
target-version = ['py38']
include = '\.pyi?$'
extend-exclude = '''
/(
  # directories
  \.eggs
  | \.git
  | \.hg
  | \.mypy_cache
  | \.tox
  | \.venv
  | build
  | dist
)/
'''