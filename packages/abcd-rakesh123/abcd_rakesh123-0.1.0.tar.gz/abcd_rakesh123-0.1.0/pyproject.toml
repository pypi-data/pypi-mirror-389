[build-system]
requires = ["setuptools", "wheel"]
build-backend = "setuptools.build_meta"

[project]
name = "abcd-rakesh123"
version = "0.1.0"
description = "abcd library with 4 modules: Association, Classification, Kmeans, Recommendation"
authors = [{name = "Your Name", email = "you@example.com"}]
readme = "README.md"
license = {text = "MIT"}
requires-python = ">=3.8"