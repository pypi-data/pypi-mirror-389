from setuptools import setup, find_packages

setup(
    name="hybrid_query_client",
    version="1.0.0",
    packages=find_packages(),
    description="Socket client for NL hybrid query engine",
)
