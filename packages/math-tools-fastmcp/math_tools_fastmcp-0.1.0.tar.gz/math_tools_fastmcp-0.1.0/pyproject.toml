#定义项目基本信息
[project]
name = "math-tools-fastmcp"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
authors = [
    { name = "tylnn68", email = "11301536+tylnn68@user.noreply.gitee.com" }
]
requires-python = ">=3.10"
dependencies = [
    "fastmcp>=2.13.0.2",
]

#创建命令行工具，运行服务的入口
[project.scripts]
math-tools-fastmcp = "math_tools_fastmcp.server:main"

#指定构建系统，打包使用的工具
[build-system]
requires = ["uv_build>=0.9.5,<0.10.0"]
build-backend = "uv_build"
