# pyDocExtractor - LLM Configuration Example
# Copy this file to config.env and fill in your actual values
# OpenAI API Configuration for Image Description

# Enable/disable LLM image description feature (default: false)
# Set to true, 1, or yes to enable
LLM_ENABLED=false

# OpenAI API endpoint (required if LLM_ENABLED=true)
# For OpenAI: https://api.openai.com/v1/chat/completions
# For Azure OpenAI: https://YOUR_RESOURCE.openai.azure.com/openai/deployments/YOUR_DEPLOYMENT/chat/completions?api-version=2024-02-15-preview
# For local models (Ollama, etc): http://localhost:11434/v1/chat/completions
LLM_API_URL=https://api.openai.com/v1/chat/completions

# API key (required if LLM_ENABLED=true)
# For OpenAI: Get from https://platform.openai.com/api-keys
# For Azure: Get from Azure Portal
# For local models: may not be required, use any value
LLM_API_KEY=your-api-key-here

# Model name (default: gpt-4-vision-preview)
# OpenAI options: gpt-4o, gpt-4o-mini, gpt-4-turbo, gpt-4-vision-preview
# Azure: your deployment name
# Local: model name (e.g., llava:13b)
LLM_MODEL_NAME=gpt-4o-mini

# Maximum number of images to describe per document (default: 5)
# Set to 0 to disable LLM features
# Set to -1 for unlimited images (no limit)
# Set to a positive number to limit images per document (helps control API costs)
LLM_MAX_IMAGES=5

# Number of context lines to include before each image (default: 100)
# These lines help the LLM understand what the image is about
LLM_CONTEXT_LINES=100

# Image resize dimension in pixels (default: 1024)
# Images are resized to this size (maintaining aspect ratio) before sending to LLM
# Larger = better quality but more tokens/cost
LLM_IMAGE_SIZE=1024

# Request timeout in seconds (default: 30)
# How long to wait for LLM API response
LLM_TIMEOUT=30

# Maximum retry attempts on failure (default: 3)
# Number of times to retry if LLM API call fails
# Minimum value is 1
LLM_MAX_RETRIES=3

# Prompt template for image description (default: see below)
# This prompt is used when context is available. The system will format it as:
# "You are analyzing a document. Based on the following context: {context}\n\n{LLM_PROMPT}"
#
# RECOMMENDED: For longer or structured prompts, use system_prompt.ini instead
# See system_prompt.ini.example for examples and documentation
#
# Priority order:
#   1. system_prompt.ini file (if exists) - RECOMMENDED for longer prompts
#   2. LLM_PROMPT environment variable (below) - for backward compatibility
#   3. Default fallback prompt - if neither is set
#
# Tips for customization:
# - Keep it concise to save tokens
# - Mention the document type if you process specific formats
# - Adjust the detail level (brief, detailed, technical, etc.)
# - Specify output format if needed (bullet points, paragraph, etc.)
# - For multiline or complex prompts, consider using system_prompt.ini
LLM_PROMPT="Please describe the image in detail, which could be (map, tables, handwritten, or images). 1. If is handwritten text, get the text as it is. 2. If is a map interpret it, and if given use the map legend. 3. If is a table identify all numerical and categorical columns, calculate for each numerical column (min, max, mean, sum), get for each categorical, unique values, mode and distribution. Also output a sample of 10 elements of the table. 4. If is image describe with a concise (2-3 sentences)."

# Example prompts for different use cases:
#
# For technical documentation:
# LLM_PROMPT=Describe this technical diagram or screenshot, focusing on its relevance to the surrounding documentation. Identify key components, labels, or UI elements. Keep it concise (2-3 sentences).
#
# For academic papers:
# LLM_PROMPT=Describe this figure or chart in academic terms, explaining how it supports the surrounding research content. Mention data trends, experimental results, or theoretical concepts shown. Keep it concise (2-3 sentences).
#
# For business reports:
# LLM_PROMPT=Describe this chart, graph, or image in business terms, relating it to the surrounding report content. Focus on key metrics, trends, or business insights. Keep it concise (2-3 sentences).
#
# For detailed analysis:
# LLM_PROMPT=Provide a detailed description of this image, analyzing all visible elements and their relationship to the surrounding content. Explain the significance and context. Aim for 4-5 sentences with specific details.
