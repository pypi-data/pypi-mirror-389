[package]
name = "backend"
version = "0.1.0"
edition = "2024"

[lib]
name = "backend"
crate-type = ["cdylib"]

[dependencies]
burn = { version = "0.19.0", features = ["wgpu"] }
pyo3 = { version = "0.27.1", features = ["extension-module"] }
