[project]
name = "tec-cli"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.13"
dependencies = [
    "mecom",
    "typer>=0.20.0",
]

[build-system]
requires = ["uv_build>=0.9.6,<0.10.0"]
build-backend = "uv_build"

[project.scripts]
tec = "tec_cli.__main__:main"

[tool.uv.sources]
mecom = { git = "https://github.com/meerstetter/pyMeCom.git" }
