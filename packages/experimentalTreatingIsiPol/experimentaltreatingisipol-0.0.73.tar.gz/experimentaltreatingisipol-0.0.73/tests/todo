[X] - O argumento de nome de máquina para um nome neutro. 
[X] - Se foi passado x_min e x_max, e linearRegionSearchMethod é 'Deterministic', gerar um warning ao usuário, que x_max e x_min não surtirão efeitos;
[X] - Avaliar a necessidade de passar o materialType; o tipo de norma (calculus_method) deveria ser utilizada para governar a análise dos gráficos, caso não seja passada nenhuma norma, utilizar o offset e exportar uma tensão de escoamento e uma tensão máxima;
[X] - Criar um parâmetro de 'testType', para passar o tipo de esforço gerado no ensaio: tração, cisalhamento, compressão, flexão. Pode ficar em redundância com a norma, e isso é esperado e sem problemas, pois pode ser que se deseje não utilizar nenhuma norma e aplicar algum critério customizado. Fora que existe mais de uma norma para um mesmo tipo de teste. O programa precisa saber como apresentar os dados (para não colocar um $E$ no lugar de um $G$), e é mais fácil filtrar pelo tipo de teste, do que pela norma;
[X] - Criar um parâmetro para alterar a região do cálculo do módulo, mesmo utilizando a norma.
[X] - Criar uma verificação quanto ao tipo de norma utilizada. Se o tipo de norma não condizer com o tipo de teste destinado, gerar um warning ao usuário; 
[X] - Modificar o sentido do argumento "direction"; Assumir a direção das fibras como principal. Logo, 'tensile-1' é na direção das fibras, 'tensile
-2' é na direção perpendicular, 'shear-12' é 12, 'shear-21' é 21, e assim por diante.  
[ ] - Criar um método para retornar todas as normas implementadas até o momento.
[ ] - Criar uma verificação se a norma passada existe entre as implementadas;
[X] - Além de checar o tipo de configuração de arquivo, checar se faz sentido calcular o poisson, por exemplo, após checar o testType
[X] - Se não foi passda nenhuma norma, utilizar limites definidos pelo usuário
[X] - Fazer gráfico para ilustra a região de cálculo do módulo
[X] - Criar arquivo para análise em flexão
[X] - No caso de flexão, gerar duas curvas: uma com força X deflexão, e outra  com tensão e deformação, e indicar os limites utilizados para o cálculo das constantes
[X] - Construir um gráfico para compararar a exclusão inicial dos dados 
[X] - Implementar cálculo de flexão
[X] - Implementar cálculo de cisalhamento
[X] - Implementar cálculo de Compressão (Apenas a resistência)
[ ] - Padronizar o método _beta_data_aquisition (colocar para excetuar o generalDataAquisition)

