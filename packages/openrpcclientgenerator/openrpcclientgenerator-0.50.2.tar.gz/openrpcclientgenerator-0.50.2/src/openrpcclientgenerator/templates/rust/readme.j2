# {{ project_title }} HTTP Client

{{ project_title }} Rust HTTP client.

## Example Usage

```rust
fn main() {
    let client = match &self.client.unrwap();
    let result = client.method.unrwap();
    println!("{}", result);
}
```
