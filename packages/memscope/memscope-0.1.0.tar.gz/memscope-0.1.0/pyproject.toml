[project]
name = "memscope"
version = "0.1.0"
description = "Analyze and simulate dynamic memory usage in C/C++ code"
authors = [{name = "Miriam Kanner", email = "m0534147159@email.com"}]
readme = "README.md"
license = {text = "MIT"}
dependencies = ["matplotlib"]

[build-system]
requires = ["setuptools"]
build-backend = "setuptools.build_meta"
