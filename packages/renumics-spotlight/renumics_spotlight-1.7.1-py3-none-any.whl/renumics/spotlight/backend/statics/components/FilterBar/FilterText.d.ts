import { FunctionComponent } from 'react';
import { Filter } from '../../types';
export interface Props {
    filter: Filter;
    className?: string;
}
declare const FilterText: FunctionComponent<Props>;
export default FilterText;
