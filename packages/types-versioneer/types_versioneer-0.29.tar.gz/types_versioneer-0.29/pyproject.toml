[build-system]
requires = ["setuptools"]
build-backend = "setuptools.build_meta"

[project]
name = "types-versioneer"
description = "Minimal stubs for versioneer."
dynamic = ["readme"]
requires-python = ">=3.8"
version="0.29"

[tool.setuptools.dynamic]
readme = {file = ["README.md"], content-type = "text/markdown"}
