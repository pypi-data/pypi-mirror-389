human_review = true
default_target = []  # SANE-exception doesn't have specific default targets

[special.relicense]
protect_scope = ["DYNAMIC_LINKING", "STATIC_LINKING"]
escape_scope = []
target = ["public-domain"]
