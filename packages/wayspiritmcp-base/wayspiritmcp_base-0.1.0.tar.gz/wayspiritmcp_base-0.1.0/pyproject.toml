[project]
name = "wayspiritmcp-base"
version = "0.1.0"
description = "weather server for baseline"
readme = "README.md"
requires-python = ">=3.13"
dependencies = [
    "httpx>=0.28.1",
    "mcp>=1.20.0",
]
[project.scripts]
wayspiritmcp-base = "main:main"