fail_fast: true

repos:
  - repo: local
    hooks:
      - id: make-quality
        name: Run make quality
        entry: make quality
        language: system
        pass_filenames: false
        stages: [pre-commit, pre-push]

      # - id: pytest
      #   name: Run pytest
      #   entry: pytest -q
      #   language: system
      #   pass_filenames: false
      #   stages: [pre-push]
      #   types: [python]
