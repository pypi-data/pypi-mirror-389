[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"


[project]
name = "timestep"
version = "2026.0.2"
description = "Multi-model provider implementations for OpenAI Agents, supporting both OpenAI and Ollama models"
readme = "README.md"
license = { text = "MIT" }
requires-python = ">=3.11"
dependencies = [
    "ollama>=0.6.0",
    "openai-agents>=0.4.2",
]
