[build-system]
requires = ["hatchling >= 1.26"]
build-backend = "hatchling.build"

[project]
name = "sigmap-polygeohasher"
version = "0.0.3"
authors = [
  { name="Hubaux William" },
]
description = "Python package that allow you to download and manipulate geometries by subdivising it using geohash."
readme = "../README.md"
requires-python = ">=3.12"
classifiers = [
    "Programming Language :: Python :: 3",
    "License :: OSI Approved :: BSD License",
    "Operating System :: OS Independent",
    "Topic :: Scientific/Engineering :: GIS",
    "Intended Audience :: Developers",
    "Intended Audience :: Science/Research"
]
license = {text = "BSD 3-Clause"}
license-files = ["LICEN[CS]E*"]
dependencies = [
    "geopandas>=1.1.1",
    "shapely>=2.1.2",
    "requests>=2.32.5",
    "numpy>=2.3.3",
    "pandas>=2.3.3",
    "matplotlib",
]

[tool.hatch.build.targets.wheel]
packages = ["src/sigmap"]

[project.urls]
Homepage = "https://github.com/WillHCode/Sigmap-PolyGeohasher"
Documentation = "https://sigmap-polygeohasher.readthedocs.io/"
Issues = "https://github.com/WillHCode/Sigmap-PolyGeohasher/issues"

[project.optional-dependencies]
docs = [
    "sphinx>=7.0.0",
    "sphinx-book-theme>=1.0.0",
    "sphinx-autodoc-typehints>=1.24.0",
]
