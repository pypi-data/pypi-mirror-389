[project]
name = "cppark"
version = "0.0.2"
description = "Your C++ tools, all in one place."
readme = "README.md"
requires-python = ">=3.10,<4"
dependencies = []

[project.scripts]
cppark = "cppark.main:main"
