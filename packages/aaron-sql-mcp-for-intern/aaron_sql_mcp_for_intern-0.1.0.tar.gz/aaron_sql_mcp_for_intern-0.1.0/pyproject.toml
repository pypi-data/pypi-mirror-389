[project]
name = "aaron-sql-mcp-for-intern"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
authors = [
    { name = "aaronuslife", email = "aaronuslife@gmail.com" }
]
requires-python = ">=3.12"
dependencies = [
    "mcp[cli]>=1.20.0",
]

[project.scripts]
aaron-sql-mcp-for-intern = "aaron_sql_mcp_for_intern:main"

[build-system]
requires = ["uv_build>=0.9.7,<0.10.0"]
build-backend = "uv_build"
