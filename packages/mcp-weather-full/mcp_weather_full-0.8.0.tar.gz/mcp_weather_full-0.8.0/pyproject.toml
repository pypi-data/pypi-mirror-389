[project]
name = "mcp-weather-full"
version = "0.8.0"
description = "MCP tools for weather"
readme = "README.md"
requires-python = ">=3.10"
dependencies = [
    "httpx>=0.28.1",
    "mcp>=1.12.2",
]

[project.scripts]
mcp-weather-full = "main:main"

