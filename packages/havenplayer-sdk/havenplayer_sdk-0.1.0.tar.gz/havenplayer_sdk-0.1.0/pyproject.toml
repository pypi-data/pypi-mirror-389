[build-system]
requires = ["setuptools>=61.0"]
build-backend = "setuptools.build_meta"

[project]
name = "havenplayer-sdk"
version = "0.1.0"
description = "livekit-rtc for Haven Player"
readme = "README.md"
requires-python = ">=3.8"
license = { text = "MIT" }
dependencies = []