name: TypeScript TAG Validation
on:
  push:
    branches: [ develop, main ]
jobs:
  validate-tags:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v3
      - name: Validate TypeScript TAGs
        run: echo "Validating TypeScript TAGs"
