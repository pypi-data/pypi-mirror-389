name: Python TAG Validation
on:
  push:
    branches: [ develop, main ]
jobs:
  validate-tags:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v3
      - name: Validate Python TAGs
        run: echo "Validating Python TAGs"
