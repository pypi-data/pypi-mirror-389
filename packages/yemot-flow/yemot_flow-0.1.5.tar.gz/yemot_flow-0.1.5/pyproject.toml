[project]
name = "yemot-flow"
version = "0.1.5"
description = "Python IVR flow for Yemot HaMashiach"
readme = "README.md"
authors = [
    { name = "davidTheDeveloper", email = "dev@code-chai.com" }
]
requires-python = ">=3.11"
dependencies = ["flask>=2.3"]

[project.scripts]
yemot-flow = "yemot_flow:main"

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"
