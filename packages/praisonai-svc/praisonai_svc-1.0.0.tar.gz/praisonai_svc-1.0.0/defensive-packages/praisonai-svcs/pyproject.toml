[project]
name = "praisonai-svcs"
version = "0.0.1"
description = "⚠️ TYPO - Install 'praisonai-svc' instead"
readme = "README.md"
authors = [
    { name = "MervinPraison" }
]
requires-python = ">=3.11"
license = "MIT"
dependencies = ["praisonai-svc>=1.0.0"]

[project.urls]
Homepage = "https://github.com/MervinPraison/PraisonAI-SVC"
Repository = "https://github.com/MervinPraison/PraisonAI-SVC"

[build-system]
requires = ["setuptools>=61.0"]
build-backend = "setuptools.build_meta"

[tool.setuptools]
py-modules = []
