[build-system]
requires = ["setuptools>=61.0"]
build-backend = "setuptools.build_meta"

[project]
name = "textkey-extractor"
version = "0.1.0"
authors = [
  { name="Devansh Patel", email="devansh.hdp@gmail.com" }
]
description = "A lightweight RAKE-style keyword extraction tool in pure Python."
readme = "README.md"
license = { text = "MIT License" }
requires-python = ">=3.7"
dependencies = ["numpy"]