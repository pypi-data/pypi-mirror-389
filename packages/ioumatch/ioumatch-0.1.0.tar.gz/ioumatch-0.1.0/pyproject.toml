[project]
name = "ioumatch"
version = "0.1.0"
description = "Compute IoU matrices and one-to-one matching for segmentation evaluation"
readme = "README.md"
license = { text = "MIT" }
authors = [{ name = "Alexis Yehadji" }]
requires-python = ">=3.8"
dependencies = []

[build-system]
requires = ["setuptools>=68", "wheel"]
build-backend = "setuptools.build_meta"
