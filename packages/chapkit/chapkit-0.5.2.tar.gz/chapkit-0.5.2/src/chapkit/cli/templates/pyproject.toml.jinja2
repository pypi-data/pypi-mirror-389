[project]
name = "{{ PROJECT_SLUG }}"
version = "0.1.0"
description = "{{ PROJECT_DESCRIPTION }}"
requires-python = ">=3.13"
dependencies = [
    "chapkit>={{ CHAPKIT_VERSION }}",
    "pandas>=2.3.3",
]

[dependency-groups]
dev = [
    "uvicorn[standard]>=0.30.0",
]
