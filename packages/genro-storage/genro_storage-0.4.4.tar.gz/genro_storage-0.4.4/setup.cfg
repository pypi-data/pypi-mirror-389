[tool:pytest]
testpaths = tests
python_files = test_*.py
addopts = -v --cov=genro_storage --cov-report=term-missing
markers = 
	integration: Integration tests that require external services (MinIO, etc.)
	unit: Fast unit tests with no external dependencies

[egg_info]
tag_build = 
tag_date = 0

