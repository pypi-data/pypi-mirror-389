[build-system]
requires = ["setuptools", "wheel"]
build-backend = "setuptools.build_meta"

[project]
name = "ryomath"
version = "0.1.0"
description = "A minimal multiplication package for PyPI demo."
authors = [{ name = "Aryan", email = "aryan.mullick@adaglobal.com" }]
readme = "README.md"
license = { text = "MIT" }
requires-python = ">=3.6"
